{
  "name": "cv",
  "version": "1.0.0",
  "scripts": {
    "build:html": "tsx src/render/html.ts",
    "build:md": "tsx src/render/md.ts",
    "build": "npm run build:html && npm run build:md",
    "clean": "rm -rf output/*"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "zod": "^4.3.6"
  }
}
